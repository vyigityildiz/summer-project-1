#pragma once

#include <SDL2/SDL.h>
#include "Texture.h"
#include "Character.h"

class Level {
    private:
    public:
        Level();
        void createLevel();
        // TODO these methods
        void renderLevel();
};