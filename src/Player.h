#pragma once

#include <SDL2/SDL.h>
#include "Character.h"

class Player : public Character {
    private:
        // TODO change this to the real player
    public:
        Player();
        Player(int xPos, int yPos);
        
        // TODO move methods
};